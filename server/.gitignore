node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Private uploaded media (never versioned, never served statically)
/storage

# Screenshots written by the end-to-end tests (debugging aid)
/tests/e2e/screenshots
